一、构造方法： 和一般函数一样的定义
    function Person(){

    }
    区别: 一般函数调用  函数名();
          构造函数调用  new Person();

    对象是类的一个实例， instanceof



二、原型对象
    原型是一个对象,
    类名.prototype 表示一个原型对象,存放着一个引用，指向了一个对象。
    对象.__proto__ 也表示一个原型对象，也指向了同一个原型对象。
    原型对象也是一个对象，它也保存了一个引用，指向了一个对象。

      把同一个类的对象的公共部分可以放入原型中，也可以放入原型的原型中。
      会自动向上搜索。
      

三、GC垃圾回收
    js中的垃圾回收机制，同java语言的垃圾回收机制
    js中垃圾回收效率是由游览器引擎决定的。
    垃圾即该变量不存在任何引用。
    
