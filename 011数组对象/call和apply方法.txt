一、函数对象的方法:
    我们知道,对象有自己的属性与方法,函数也是一种对象,函数也有自己的方法。

    函数对象的方法有： call();  applay();
    这两个方法的作用： 指定this,即this可以由参数来指定。

    总结this:
     (1)以函数方式调用，this是window
     (2)以方法方式调用，this是调用方法的对象
     (3)以构造方法方式调用，this是创建的对象
     (4)以call();和applay(); 方法调用，this是传递的参数。